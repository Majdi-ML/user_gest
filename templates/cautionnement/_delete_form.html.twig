<form method="post" action="{{ path('app_cautionnement_delete', {'id': cautionnement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cautionnement.id) }}">
    <button class="btn">Delete</button>
</form>
