<form method="post" action="{{ path('app_cnss_delete', {'id': cnss.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cnss.id) }}">
    <button class="btn">Delete</button>
</form>
