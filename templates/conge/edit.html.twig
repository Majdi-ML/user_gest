{% extends 'base.html.twig' %}

{% block title %}Modifier Congé{% endblock %}

{% block css %}
    {{ parent() }}
    {% include '_modern_form_styles.html.twig' %}
{% endblock %}

{% block body %}
<div class="container-fluid p-4">
    <div class="page-header">
        <div class="page-header-content">
            <div>
                <h1><i class="bi bi-pencil-square"></i> Modifier Congé</h1>
                <p class="page-subtitle">Mise à jour du congé #{{ conge.id }}</p>
            </div>
        </div>
    </div>

    <div class="form-card">
        {{ include('conge/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="action-buttons mt-3">
        <a href="{{ path('app_conge_index') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Retour
        </a>
        {{ include('conge/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
