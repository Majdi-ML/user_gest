
<form method="post" action="{{ path('app_frais_syndic_reglement_delete', {'id': frais_syndic_reglement.id}) }}" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frais_syndic_reglement.id) }}">
    <button type="button" class="btn btn-danger btn-delete">
        <i class="bi bi-trash"></i> Supprimer
    </button>
</form>