<form method="post" action="{{ path('app_status_bureau_delete', {'id': status_bureau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ status_bureau.id) }}">
    <button class="btn">Delete</button>
</form>
