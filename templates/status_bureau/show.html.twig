{% extends 'base.html.twig' %}

{% block title %}StatusBureau{% endblock %}

{% block body %}
    <h1>StatusBureau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status_bureau.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ status_bureau.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_status_bureau_index') }}">back to list</a>

    <a href="{{ path('app_status_bureau_edit', {'id': status_bureau.id}) }}">edit</a>

    {{ include('status_bureau/_delete_form.html.twig') }}
{% endblock %}
