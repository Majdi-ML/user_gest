{% extends 'base.html.twig' %}

{% block title %}TypeRassmblement index{% endblock %}

{% block body %}
    <h1>TypeRassmblement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_rassmblement in type_rassmblements %}
            <tr>
                <td>{{ type_rassmblement.id }}</td>
                <td>{{ type_rassmblement.libelle }}</td>
                <td>
                    <a href="{{ path('app_type_rassmblement_show', {'id': type_rassmblement.id}) }}">show</a>
                    <a href="{{ path('app_type_rassmblement_edit', {'id': type_rassmblement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_rassmblement_new') }}">Create new</a>
{% endblock %}
